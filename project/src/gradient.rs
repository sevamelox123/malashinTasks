fn main()
{
    // y = a0 + a1x
    // metod naimenshix kvadratov
    // Na0 + sum(x)ai + sum y
    // sum(xi)a0 + sum(x1^2)a1 = sum(xi)y



    // a0n+1 = a0n - delta * 2 * (n a0n + sum(xi)ain - sum(yi))
    // a0n+1 = ain - delta * 2 *((sum(xi)ain + sum(xi^2)an - sum(x*y)))


    //mnoshestevnnaya regresia

    //y = a0 + a1x1 + a2x2 + a3x3


    // NA0 + sum(x1i)a1 + sum(x2i)a2 + sum(x3i)a3 = sum(yi)

    // sum(x1i)a0 + sum(x1i*x1i)a1 + sum(x2i*x1i)21 + sum(x3i*x1i)3
    // 
    // bazu iz failika shtobi phochitat

    // zavisimot temperaturi ot vlashnoti i dvlenia

    // ne reshat v excel


    // метод наименьших квадратов и метод градинетного спука и метод сплайна
}